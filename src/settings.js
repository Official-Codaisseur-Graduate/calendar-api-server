const CREDENTIALS_PATH = "credentials.json"
const TOKEN_PATH = "token.json"
const SCOPES = ["https://www.googleapis.com/auth/calendar.readonly"]

module.exports = { CREDENTIALS_PATH, TOKEN_PATH, SCOPES }